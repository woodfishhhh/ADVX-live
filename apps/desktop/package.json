{
  "name": "@advx/desktop",
  "version": "0.1.0",
  "private": true,
  "main": "out/main/index.js",
  "scripts": {
    "ensure:electron": "node scripts/ensure-electron.mjs",
    "dev": "bun run ensure:electron && electron-vite dev",
    "build": "electron-vite build",
    "package:win:x64:dir": "electron-builder --win --x64 --dir",
    "smoke": "bun run ensure:electron && node scripts/smoke.mjs",
    "smoke:system-audio": "bun run ensure:electron && bun run build && node scripts/system-audio-smoke.mjs",
    "smoke:capture": "bun run ensure:electron && bun run build && node scripts/capture-smoke.mjs",
    "smoke:floating-chat": "bun run ensure:electron && bun run build && node scripts/floating-chat-smoke.mjs",
    "smoke:tray": "bun run ensure:electron && bun run build && node scripts/tray-smoke.mjs",
    "smoke:runtime": "bun run ensure:electron && bun run build && bun scripts/bun-recorded-pipeline-smoke.ts --artifact-root ../../.omx/artifacts/test-results/desktop-runtime-smoke",
    "profile:content-trace": "bun run ensure:electron && bun run build && node scripts/content-trace.mjs",
    "smoke:ai-calls": "bun run ensure:electron && bun run build && bun scripts/bun-recorded-pipeline-smoke.ts --artifact-root ../../.omx/artifacts/test-results/desktop-ai-calls && bun test ../backend-bun/src/api/recorded-pipeline.integration.test.ts",
    "smoke:ui": "bun run ensure:electron && bun run build && node scripts/ui-polish-smoke.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.node.json && tsc --noEmit -p tsconfig.web.json",
    "test": "vitest run --config ../../vitest.config.ts --project desktop-main --project desktop-preload --project desktop-renderer"
  },
  "dependencies": {
    "@advx/contracts": "workspace:*",
    "@echogarden/fvad-wasm": "0.2.0",
    "electron-log": "^5.4.4",
    "lucide-react": "^1.25.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@electron/fuses": "1.8.0",
    "@tailwindcss/vite": "^4.3.3",
    "@types/node": "24.13.3",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.6.0",
    "electron": "43.2.0",
    "electron-builder": "^26.0.0",
    "electron-vite": "4.0.1",
    "playwright-core": "^1.61.1",
    "tailwindcss": "^4.3.3",
    "typescript": "5.9.3",
    "vite": "7.3.6",
    "vitest": "^3.2.0"
  }
}
